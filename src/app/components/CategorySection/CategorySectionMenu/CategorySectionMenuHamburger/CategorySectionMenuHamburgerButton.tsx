import { IconButton } from "@/app/components/Button/IconButton"

export const CategorySectionMenuHamburgerButton = () => {
    return (
        <IconButton iconType={"hamburger-menu"} iconFillColor={"lapis"} iconSize={"9xl"} onClick={() => 1} />
    )
}